var player;
var enemy;

//playerData will be passed in the view, just grab it using this function.
function getPlayerData(){
  player = playerData;
}

//generate an enemy based on lvl, type, and zone
function generateEnemy(lvl,type,zone){

}

//called when the player presses the attack button
//enemy attacks after the player
function playerAttack(){
  //enemy.damage(player.getDamage());
  //if enemy heath > 0 then
  // player.damage()....
}
